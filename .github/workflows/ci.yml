name: CI

on:
  push:
    branches: [ master ]
  pull_request: ~

jobs:
  docker-test:
    runs-on: ubuntu-latest
    name: Docker test
    steps:
      - uses: actions/checkout@v4
      - name: Run Docker integration test
        run: make test
